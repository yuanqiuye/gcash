# gcash CLI default configuration
# Copy this file to ~/.gnucash-cli/config.yaml

# Default currency (ISO 4217)
default_currency: TWD

# Default book path
# default_book: ~/Documents/my_finance.gnucash
